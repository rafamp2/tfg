DEV_MODE: dev #user or dev, cambia la autorizacion sobre cosas que se pueden hacer
DEVICE: cuda

MODELS_DIR: ./models

EMBEDDINGS_PATH: all-MiniLM-L6-v2

VECTORDB_TYPE: chroma
VECTORDB_PATH: vectordb/chroma

TTS_PATH: src/tmp

USE_CTRANSFORMERS: False
LLM_PATH: mixtral_7bx2_moe.Q8_0.gguf
PROMPT_TYPE: mistral

LLM_CONFIG:
  MAX_NEW_TOKENS: 512
  TEMPERATURE: 0.2
  REPETITION_PENALTY: 1.1
  CONTEXT_LENGTH: 4000

BASE_RETRIEVER_CONFIG:
  SEARCH_K: 4

RERANKER_PATH: bge-reranker-base
RERANKER_TYPE: bge

RERANK_RETRIEVER_CONFIG:
  SEARCH_K: 10
  TOP_N: 4

COMPRESSION_RETRIEVER_CONFIG:
  SEARCH_K: 10
  SIMILARITY_THRESHOLD: 0.5

TEXT_SPLIT_MODE: default  
CHUNK_SIZE: 500
CHUNK_OVERLAP: 50
SEPARATORS: ["\n\n", "\n", ". ", " ", ""]

